export default function Test() {
    return ( <
        div className = "p-10 bg-primary text-white" >
        <
        h1 > Theme Test < /h1> <
        p > This should not be corrupted. < /p> <
        /div>
    );
}